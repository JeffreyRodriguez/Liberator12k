version: 0.2

batch:
  fast-fail: true

phases:
  install:
    on-failure: ABORT
    commands:
      - add-apt-repository -y ppa:openscad/releases
      - apt-get update
      - apt install -y xvfb openscad cmark-gfm
  build:
    on-failure: ABORT
    commands:
      - make all dist
artifacts:
  files:
    - '**/*'
  name: Liberator12k.zip
  discard-paths: no
  base-directory: dist
